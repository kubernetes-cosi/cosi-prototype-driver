apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: pluginregistrations.objectbucket.io
spec:
  group: objectbucket.io
  names:
    kind: PluginRegistration
    listKind: PluginRegistrationList
    plural: pluginregistrations
    singular: pluginregistration
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: PluginRegistration is the Schema for the pluginregistrations API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: PluginRegistrationSpec defines the desired state of PluginRegistration
          properties:
            host:
              type: string
            pluginName:
              type: string
          required:
          - host
          - pluginName
          type: object
        status:
          description: PluginRegistrationStatus defines the observed state of PluginRegistration
          properties:
            certSecret:
              description: SecretReference represents a Secret Reference. It has enough
                information to retrieve secret in any namespace
              properties:
                name:
                  description: Name is unique within a namespace to reference a secret
                    resource.
                  type: string
                namespace:
                  description: Namespace defines the space within which the secret
                    name must be unique.
                  type: string
              type: object
          required:
          - certSecret
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
